# MedPrint 项目工程守则与医疗领域刚性约束

本规范适用于 `medprint` 项目的所有代码开发与重构。所有代码必须严格遵循以下七大核心原则：

### 1. 物理毫米精度与纯矢量直出（绝无 DOM 栅格化降级）
- 所有内部坐标与排版计算必须统一采用物理单位（微米 `um` 或毫米 `mm`），严禁在布局排版阶段使用易受缩放干扰的 CSS 屏幕像素；
- 打印输出必须直接编译为 300/600 DPI 纯矢量 PDF 或物理打印机原始指令（ESC/P2、TSPL），条形码（Code 128、DataMatrix、QR）必须直接以矢量路径渲染，严禁使用 `html2canvas` 等位图截图方案。

### 2. 规避双服务地狱：支持“单二进制全内置”与多矩阵交付
- 杜绝传统“前端一个 npm 容器 + 本地一个 Windows 打印服务”的碎片化部署噩梦；
- 引擎必须支持四大形态交付：
  1. `medprint-server`：单个静态 Rust 二进制（通过 `rust-embed` 内置 Vue3 Web 设计器），双击即运行，内置 REST/WebSocket 与驱动通道；
  2. `packages/extension`：Chrome/Edge 浏览器扩展（基于 Native Messaging），零网络端口、零 SSL 证书困扰；
  3. `apps/desktop`：基于 Tauri 2.0 的全功能跨平台桌面端（支持统信 UOS / 麒麟 Kylin 信创与 Windows）；
  4. `packages/designer`：纯前端 WASM npm SDK，可直接嵌入现有 HIS/LIS 系统。

### 3. A5 横向双列折流平衡算法（Snaking Flow）
- 化验单默认优先支持 A5 横向（210mm x 148mm）双列排版；
- 左列自上向下排满后，内容平滑折入右列并自动克隆表头；只有当右列也排满时才允许发生分页；
- 具备单页弹性压缩机制（Auto-Compaction）：当行数溢出 1~2 行时，优先自动微调行高与字号，保证整张化验单 100% 紧凑在单页内打印完毕。

### 4. 真实双向硬件状态追踪（Spooler Callbacks）
- 必须通过 Windows Spooler API (`FindNextPrinterChangeNotification`) 与 Linux CUPS 深度监控物理打印队列；
- 必须向上层准确回传：排队中 (`QUEUED`)、传输中 (`PRINTING`)、缺纸 (`PAPER_OUT`)、卡纸 (`PAPER_JAM`)、物理出纸完成 (`COMPLETED`)，确保处方号与医保发票号核销万无一失。

### 5. 临床双轨制体验（意图向导 + AST 审查，禁止传统低代码）
- 设计器不是 Grapecity / FineReport。禁止开放物料、自由坐标源真相、通用表达式或自定义组件协议。
  - **临床向导模式（Doctor Wizard）**：选预设、开关模块、自然语言意图；AI 产出封闭 AST；零拖拽；
  - **专业画布（Pro Canvas）**：信息科审查引擎投影并覆写槽位参数，不是像素级低代码画布。
- 新增视觉类型必须先进入 Rust `ReportElement`。

### 6. 专业医学影像与特殊图表高保真呈现
- 原生支持血栓弹力图（TEG 反应时间R/K/MA纺锤曲线矢量渲染）；
- 原生支持 12 导联心电图（ECG 1mm x 1mm 绝对基准网格与波形）；
- 超声/内镜/X光支持 1/2/4/6 格自适应防拉伸拼版，保留原始高保真灰阶与彩色图谱，并支持微米级病灶标记。

### 7. 内置临床医疗公式与合规防伪签章责任链
- 内核原生内置常用临床公式计算库（eGFR、LDL-C、BMI、阴离子间隙、校正钙等），支持根据患者性别、年龄段、孕周动态匹配参考区间并自动判定偏高偏低（↑↓）与危急值；
- 支持三级医疗责任签名链（采样/检验/审核/报告），支持 CA 电子验签防篡改；
- 支持矢量检验专用红章（半透明正片叠底防遮挡文字、微偏转角随机抖动防伪），签名栏与盖章区域严格执行 `Keep-With-Next` 防孤立规则，绝不允许单独溢出空白页。
